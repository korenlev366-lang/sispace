# Obsidian vault mirror (canonical copy in SISpace)

Copied from `/home/lev/harness vault` by `harness/scripts/port-memory-to-sispace.mjs`.

| Folder | Purpose |
|--------|---------|
| `Harness/rollout-log/` | Per-rollout session summaries (one note per ROLLOUT-*) |
| `Harness/reasoning-patterns/` | Per-session pattern mirrors |
| `Harness/accepted-lessons/` | Per-proposal Obsidian stubs |

Live vault remains at `/home/lev/harness vault`; this tree is for recall when working only in SISpace.
