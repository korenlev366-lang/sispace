---
source: "cursor-harness"
tags: "harness"
proposal_id: "agent-8737f2d9-5548-43aa-a09c-a02bc54c2bdb"
date: "2026-06-04"
---

User provided a detailed, file-level spec aligned with pi.dev compaction docs. An explore subagent mapped existing session store, slash commands, sispace.yaml parser, Agent.create usage, and Obsidian read paths before implementation. Core pure functions (token estimate, cut-point finder, shouldAutoCompact) were implemented first, then wired through send-turn, slash, and Orchestrator. Initial findCompactionCutPoint placed the cut at the message that exceeded keep_recent_tokens rather than the first kept message index (firstKeptMessagePos), which would summarize too much or too little; backward walk was corrected to set cutIndex at messageIndices[firstKeptMessagePos]. Summary generation initially parsed only text-delta stream events; switching to textFromStreamEvent aligned with existing SDK stream handling. Post-compact agent lifecycle follows Pi reload: closeSessionAgent, clear cursorAgentId, inject compactionSummaryBlock once via contextInjected=false. Remaining uncertainty: verify script is static source grep only — cut-point edge cases (tool lines adjacent to boundaries, incremental compaction with previous firstKeptEntryId) are not exercised at runtime; live Agent.create summarization and Obsidian PUT were not integration-tested in session.

## Related

- [[Harness/rollout-log/ROLLOUT-20260604-224625-sdk]]
