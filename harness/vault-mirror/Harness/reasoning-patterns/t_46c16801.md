---
source: "cursor-harness"
tags: "harness"
proposal_id: "t_46c16801"
date: "2026-06-03"
---

Researcher curl-probed the live REST API and found search integrations used GET while the plugin requires POST — explaining broken lesson injection and vault search. Vault scan showed ~139 flat mirror files with almost no wikilinks inside Harness/, so the graph looked disconnected despite a healthy backend. Architect locked scope: Phase A restore POST search; Phase B path-qualified links, SyncEntry.links/appendLinksSection cross-links, hub MOCs; explicitly keep one vault (/home/lev/harness vault) with Harness/ + SISpace/tasks/ folders. Coder implemented across Rust, Python hook, TS sync lib, rebuilt dist/, updated docs and task note. Reviewer flagged that sync ## Related is code-correct but vault-unproven because ROLLOUT synced at 03:54:54Z predates the dist rebuild — a timing/backfill gap, not a logic bug. Tester added unit tests closing reviewer nits on appendLinksSection idempotency but E2E sync-link and live-search criteria still need OBSIDIAN_API_KEY and one post-task chain run. Subagent MCP catalog empty is an environment constraint, not fixable in pipeline code.
- [[Harness/rollout-log/ROLLOUT-20260603-070753-sdk]]
