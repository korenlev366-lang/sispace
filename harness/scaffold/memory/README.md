# Memory Ledger Overview

See the tracked stub at `harness/memory/README.md` in the repo root after clone. This scaffold copy is what `harness-bootstrap.sh` seeds from.

Run `npm run harness:bootstrap` to populate local `harness/memory/` (gitignored).
